// The compiled version of the file with-typescript.ts to with-typescript.js

// Function that takes two arguments (a and b) and returns their sum.
function add(a, b) {
  return a + b;
}

// Calling the add function with arguments 2 and 5, and storing the result in the 'result' variable.
var result = add(2, 5);

// Outputting the value stored in 'result' to the console.
console.log(result);
